{
  "providers": ["node"],
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_22", "python3"]
    },
    "install": {
      "cmds": ["npm install"]
    },
    "start": {
      "cmd": "npm run preview -- --port $PORT --host"
    }
  }
}
